name: Checks

on:
  push:
    branches:
      - '**'  # This will trigger on all branches
    tags:
      - '**'  # This will trigger on all tags

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Disable incremental compilation for faster from-scratch builds
  CARGO_INCREMENTAL: 0
  CARGO_TERM_COLOR: always
  BRANCH: ${GITHUB_REF#refs/heads/}
  REPO: ${{ github.repository }}
  ORG: ${{ github.repository_owner }}

jobs:
#  test:
#    name: cargo test
#    runs-on: ubuntu-24.04
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions-rust-lang/setup-rust-toolchain@v1
#      - run: cargo test --all-features
#
#  # Check formatting with rustfmt
#  formatting:
#    name: cargo fmt
#    runs-on: ubuntu-24.04
#    steps:
#      - uses: actions/checkout@v4
#      # Ensure rustfmt is installed and setup problem matcher
#      - uses: actions-rust-lang/setup-rust-toolchain@v1
#        with:
#          components: rustfmt
#      - name: Rustfmt Check
#        uses: actions-rust-lang/rustfmt@v1

  build_and_push:
    name: Build Image
    runs-on: ubuntu-24.04
    #if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/dev'
    #needs: ["test", "lint"]
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Log in to GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build \
          --build-arg GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }} \
          --build-arg GITHUB_USERNAME=${{github.actor}} \
          -t ghcr.io/${ORG}/${REPO}:${BRANCH}-${GITHUB_SHA} \
          .

      - name: Push Docker Image
        run: |
          docker push ghcr.io/${ORG}/${REPO}:${BRANCH}-${GITHUB_SHA}

  deploy:
    name: Build Image
    runs-on: ubuntu-24.04
    #if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/dev'
    needs: ["build_and_push"]
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout tools repo
        uses: actions/checkout@v4
        with:
          repository: twinexyz/argocd
          path: argocd
          token: ${{ secrets.PAT_TOKEN  }}

      - name: Update Tag in Helm Values for argocd
        run: |
            cd apps/values/${REPO} &&  sed -i "/tag:/s/tag:.*/tag: ${BRANCH}-${GITHUB_SHA}/" staging-api.yaml && sed -i "/tag:/s/tag:.*/tag: ${BRANCH}-${GITHUB_SHA}/" staging-indexer.yaml
            git config --global user.email "devops@twinelabs.xyz"
            git config --global user.name "twinebot"
            git add .
            git commit --allow-empty -m "updated image tag to ${SHORT_SHA} for ${REPO_NAME}"
            git push origin main
